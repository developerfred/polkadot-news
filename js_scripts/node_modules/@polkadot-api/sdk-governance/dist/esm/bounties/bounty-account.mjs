import { Binary, AccountId } from 'polkadot-api';
import { u32 } from 'scale-ts';

const ZERO = new Array(32).fill(0);
const createId = (...parts) => {
  const arr = [...ZERO];
  let i = 0;
  parts.forEach((p) => p.forEach((v) => arr[i++] = v));
  return new Uint8Array(arr);
};
const bountyIdPrefix = Binary.fromText("modlpy/trsry\bbt").asBytes();
const getBountyAccount = (id) => AccountId().dec(createId(bountyIdPrefix, u32.enc(id)));
const childBountyIdPrefix = Binary.fromText("modlpy/trsry\bbc").asBytes();
const getChildBountyAccount = (parentId, id) => AccountId().dec(createId(childBountyIdPrefix, u32.enc(parentId), u32.enc(id)));

export { getBountyAccount, getChildBountyAccount };
//# sourceMappingURL=bounty-account.mjs.map
